# Application Configuration
PORT=8080
NODE_ENV=development

# Azure Cosmos DB Configuration (optional)
# If set, Azure Cosmos DB with MongoDB API will be used for task storage
# Otherwise, in-memory storage is used
# This should be configured in Azure Web App Configuration, not in a .env file
AZURE_COSMOS_CONNECTIONSTRING=

# Azure Service Bus (Queue) Configuration (optional)
# If set, task lifecycle events will be published to the configured queue
AZURE_SERVICEBUS_CONNECTIONSTRING=
AZURE_SERVICEBUS_QUEUE_NAME=

# Optional: specify database and collection names
# MONGODB_DATABASE=tasks_db
# MONGODB_COLLECTION=tasks
