# Application Configuration
PORT=8080
NODE_ENV=development

# MongoDB Configuration (optional)
# If set, MongoDB will be used for task storage; otherwise, in-memory storage is used
MONGODB_URI=
# Optional: specify database and collection names
# MONGODB_DATABASE=tasks_db
# MONGODB_COLLECTION=tasks
