{
  "name": "azure",
  "version": "1.0.0",
  "description": "Azure Function for sending task requests to Service Bus Queue",
  "main": "dist/src/functions/*.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "build:production": "npm run clean && npm run build",
    "clean": "rm -rf dist",
    "start:func": "func start",
    "watch": "tsc --watch"
  },
  "keywords": [
    "node",
    "typescript",
    "azure",
    "queue",
    "serverless"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@azure/functions": "^4.10.0",
    "@azure/service-bus": "^7.9.5"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
